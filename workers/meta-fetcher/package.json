{
  "name": "visualist-meta-fetcher",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/meta.js && node --check src/index.js",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
